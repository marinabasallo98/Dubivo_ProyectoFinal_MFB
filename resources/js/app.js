//Cargamos la configuración de Laravel
import './bootstrap';

//Inicializamos cuando la página esté lista
document.addEventListener('DOMContentLoaded', function() {
    console.log('✅ Aplicación lista');
    
    //Los componentes se auto-inicializan desde sus propios archivos
    //(actorfilters.js, schoolfilters.js, workfilters.js)
    
    //No necesitamos lógica adicional aquí
    //Cada página solo carga los componentes que necesita
});